subprojects {
    group = 'uk.gov.justice.laa.claimforpayment'
    configurations.all {
        resolutionStrategy { //overrides for snyk vulnerabilities
            force 'org.apache.tomcat.embed:tomcat-embed-core:11.0.9'
            force 'org.apache.commons:commons-lang3:3.18.0'
        }
    }
}


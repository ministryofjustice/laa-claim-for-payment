logging.level.root: INFO
security:
  enabled: true   # feature-flag flip to true when the client is ready
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ISSUER_BASE_URL}
          audience: ${AUDIENCE:api-audience}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mock-oidc.fullname" . }}-test-connection"
  labels:
    {{- include "mock-oidc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mock-oidc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
